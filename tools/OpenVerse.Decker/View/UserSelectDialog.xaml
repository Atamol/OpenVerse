<UserControl x:Class="OpenVerse.Decker.View.UserSelectDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
        <StackPanel MinWidth="320"
                    Margin="8">
                <TextBlock Name="Title"
                           Text="{StaticResource SelectUserKeyTitle}"
                           TextWrapping="Wrap"
                           FontSize="20"
                           Margin="0,0,0,5"/>
                <TextBlock x:Name="MessageText"
                           TextWrapping="Wrap"
                           FontSize="14"
                           Margin="0,0,0,16"
                           Visibility="Collapsed"/>
                <ListBox x:Name="ChoiceList"
                         MinHeight="120"
                         Margin="0,0,0,16"
                         SelectionChanged="ChoiceList_SelectionChanged"/>
                <StackPanel Orientation="Horizontal"
                            HorizontalAlignment="Right">
                        <Button x:Name="GoButton"
                                Padding="14,6"
                                Click="GoButton_Click"/>
                        <Button x:Name="ExitButton"
                                Content="{StaticResource CloseDueToUserKey}"
                                Padding="14,6"
                                Visibility="Collapsed"
                                Click="ExitButton_Click"/>
                </StackPanel>
        </StackPanel>
</UserControl>
