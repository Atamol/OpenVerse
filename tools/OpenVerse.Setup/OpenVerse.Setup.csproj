<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <AssemblyName>openverse-setup</AssemblyName>
    <RootNamespace>OpenVerse.Setup</RootNamespace>
    <InvariantGlobalization>true</InvariantGlobalization>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\src\OpenVerse.Common\OpenVerse.Common.csproj" />
  </ItemGroup>

  <ItemGroup>
    <!-- reads Unity AssetBundle (.unity3d) files - the client's own downloaded master text data, never bundled here -->
    <PackageReference Include="AssetsTools.NET" Version="3.0.5" />
  </ItemGroup>

</Project>
